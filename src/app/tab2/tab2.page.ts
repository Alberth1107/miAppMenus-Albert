import { Component } from '@angular/core';

/**
 * `tab2.page.ts`
 *
 * Página ``Información personal`` — aquí puedes mostrar datos del autor,
 * biografía breve, objetivos del proyecto o información relevante solicitada
 * por la evaluación. Documenta cada campo y por qué aparece en la pantalla.
 *
 * Recomendación para la entrega: escribe en primera persona cómo implementaste
 * la carga de datos (por ejemplo: "Los datos personales se guardan en un
 * objeto literal y se enlazan en la plantilla con binding de Angular").
 */
@Component({
  selector: 'app-tab2',
  templateUrl: 'tab2.page.html',
  styleUrls: ['tab2.page.scss'],
  standalone: false,
})
export class Tab2Page {

  // Añadir aquí propiedades públicas que la plantilla consuma (p. ej. nombre,
  // descripción). Documenta el propósito de cada una.
  constructor() {}

}
