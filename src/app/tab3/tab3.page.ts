import { Component } from '@angular/core';

/**
 * `tab3.page.ts`
 *
 * Página ``Contacto`` — muestra métodos de contacto o enlaces. Documenta
 * claramente la fuente de los datos (si son estáticos o vienen de un servicio)
 * y explica cómo el usuario puede interactuar con los elementos (p. ej. enviar
 * un correo o abrir un enlace).
 */
@Component({
  selector: 'app-tab3',
  templateUrl: 'tab3.page.html',
  styleUrls: ['tab3.page.scss'],
  standalone: false,
})
export class Tab3Page {

  // Si añades un formulario de contacto, comenta el propósito de cada campo
  // y cómo validas la entrada del usuario.
  constructor() {}

}
