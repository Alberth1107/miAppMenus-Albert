import { Component } from '@angular/core';

/**
 * `tab1.page.ts`
 *
 * Página ``Inicio`` — contiene la lógica y datos visibles en la pestaña inicial.
 * Aquí se recomienda documentar, en tus propias palabras, qué hace cada
 * variable o método que añadas (por ejemplo: cómo se cargan las imágenes,
 * qué pasa al pulsar un botón, o cómo se navega a otra vista).
 *
 * Para la evaluación: añade comentarios explicativos en primera persona
 * (ej.: "Esta función carga las tarjetas de los jugadores desde un array
 * definido localmente para mostrar la información relevante").
 */
@Component({
  selector: 'app-tab1',
  templateUrl: 'tab1.page.html',
  styleUrls: ['tab1.page.scss'],
  standalone: false,
})
export class Tab1Page {

  // Constructor sin lógica por defecto. Añade aquí la inyección de servicios
  // si necesitas acceder a APIs, almacenamiento o navegación.
  constructor() {}

  // Ejemplo: si añades un método que devuelve las tarjetas de jugadores,
  // documenta qué datos contiene el array y por qué se eligieron.

}
